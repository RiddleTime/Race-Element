<UserControl x:Class="RaceElement.Controls.GameSettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RaceElement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Height="35" VerticalAlignment="Top" x:Name="buttonRestartApp">Apply Settings (Restarts Race Element)</Button>

        <StackPanel Grid.Row="1" Orientation="Vertical" x:Name="stackPanelSettings" HorizontalAlignment="Stretch">

        </StackPanel>
    </Grid>
</UserControl>
